[build]
  command = "npm cache clean --force && rm -rf node_modules && npm install --include=dev && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22.13.1"
  NPM_FLAGS = "--include=dev"

[[plugins]]
  package = "@netlify/plugin-nextjs"
