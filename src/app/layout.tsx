/**
 * Root Layout - Redirige vers la locale par défaut
 * Le vrai layout est dans /[locale]/layout.tsx
 */

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
